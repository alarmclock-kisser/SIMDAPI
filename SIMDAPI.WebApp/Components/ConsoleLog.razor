@inject Shared.AppState AppState

<div class="console-log bg-dark text-white p-2 mt-4 rounded" style="font-family: monospace; max-height: 300px; overflow-y: auto;">
    @foreach (var entry in AppState.Logs)
    {
        <div>@entry</div>
    }
</div>
