@inject Shared.AppState AppState

<button class="btn btn-secondary mb-3" @onclick="Toggle">
    @((AppState.IsDarkMode ? "☀️" : "🌙") + " Theme")
</button>

@code {
    private void Toggle() => AppState.ToggleDarkMode();
}
